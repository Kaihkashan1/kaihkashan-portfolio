---
import "../styles/global.css";
import HeadSEO from "../components/HeadSEO.astro";

interface Props {
	title: string,
}

const { title } = Astro.props as Props;
---

<!doctype html>
<html lang="en" style="scroll-behavior: smooth">
	<HeadSEO title={title} slug={Astro.url.pathname} />
	<body>
		<slot />
	</body>
</html>
